# Copy this to .env.local for local dev (leave empty — Vite proxy handles it)
# Set this in Railway frontend service environment variables for production

# Backend Railway service URL (no trailing slash)
# e.g. https://imocha-dashboard-backend.up.railway.app
VITE_API_URL=
